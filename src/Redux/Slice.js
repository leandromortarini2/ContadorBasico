/* eslint-disable no-unused-vars */
import { createSlice } from "@reduxjs/toolkit";

const initialState = 0;

export const contadorSlice = createSlice({
  name: "Contador",
  initialState,
  reduce: {
    setContador: (initialState, action) => {
      initialState = 
    },
  },
});
